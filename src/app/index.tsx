import { InstrumentEmptyState } from '@/components/instrument-empty-state';

export default function TunerScreen() {
  return <InstrumentEmptyState title="Tuner" supporting="Play a note when you are ready." />;
}
