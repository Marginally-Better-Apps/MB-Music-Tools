import { InstrumentEmptyState } from '@/components/instrument-empty-state';

export default function MetronomeScreen() {
  return (
    <InstrumentEmptyState title="Metronome" supporting="Set a tempo when you are ready." />
  );
}
