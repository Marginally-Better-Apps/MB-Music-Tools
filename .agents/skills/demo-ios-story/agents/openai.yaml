interface:
  display_name: "iOS Story Demo"
  short_description: "Record and publish iOS story acceptance"
  default_prompt: "Use $demo-ios-story to implement an issue, record acceptance locally with ./scripts/record-demo.sh, and open a PR against main for Autoloader CI."
